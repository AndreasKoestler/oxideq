# Progress Log

Ralph-loop iteration log for OxidEQ. Append an `## Iteration N` section per
iteration; keep each to 3–5 bullets: what was done, key decisions, gotchas,
and any deferred verifications (e.g. Linux-only PipeWire checks deferred on
the macOS dev machine). Task 4 iterations must record the measured bench and
perf-gate numbers as the machine's NFR baseline.

_No iterations yet._

## Iteration 1

- Ran `cargo init --name oxideq` in the existing (non-empty) repo, then `cargo add anyhow biquad cpal regex ringbuf`; set `edition = "2021"` (cargo defaulted to `"2024"`) and added `description` + `[profile.release]` (lto, codegen-units=1) to `Cargo.toml`.
- Resolved dependency versions: `anyhow 1.0.104`, `biquad 0.6.0`, `cpal 0.18.1`, `regex 1.13.1`, `ringbuf 0.5.1`. `biquad` cleared the `>= 0.4.2` floor with room to spare, so no version pin was needed; verified in source that `Type<DBGain>::{LowShelf, HighShelf, PeakingEQ}` all carry the gain parameter.
- Wrote `src/lib.rs` (crate doc comment only), `src/main.rs` (placeholder dispatcher print), `presets/example.txt` (10-band AutoEQ-style fixture: 1 LSC, 8 PK, 1 HSC, preamp -6.8 dB), `.githooks/pre-commit` (fmt --check, clippy -D warnings, test), and ran `git config core.hooksPath .githooks`.
- Verified `cargo build`, `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` all pass clean (0 tests, 0 failures) before committing; `Cargo.lock` committed since this is a binary application.
- Gotcha: `cargo init` in a non-empty directory works fine but defaults `edition` to `"2024"` regardless of installed toolchain support — must explicitly override to `"2021"` per the plan every time this pattern is reused.

## Iteration 2

- TDD: wrote `src/preset.rs` with only the `#[cfg(test)]` module (7 tests) first, confirmed RED via `cargo test preset` (compile errors: `Band`/`FilterKind`/`parse` undefined), then prepended the parser implementation (regex-based: `preamp_re`, `loose_re` to classify ON/OFF and filter type, `strict_re` to validate/extract args) and confirmed GREEN (7 passed).
- Created `presets/koss_porta_pro.txt` and `presets/meze_99_classics.txt` as exact-match fixtures (real AutoEQ results) covering decimal Fc, boost/cut shelves, low/high Q, extreme gains, and a near-zero HSC gain; added `pub mod preset;` to `src/lib.rs`.
- Parsing rules implemented exactly per plan: `Preamp:` sets `preamp_db` (default 0.0 if absent); `ON PK/LSC/HSC` → `Band`; `OFF` and unsupported types (e.g. `LP`) → skipped with a line-numbered warning; a supported type with malformed args → hard `bail!` error; all other lines ignored silently.
- Gotcha: the brief's verbatim code block is not byte-identical to `rustfmt` output (line-wrapping of `assert_eq!`/`push(format!(...))` calls) — ran `cargo fmt` after pasting to satisfy the pre-commit hook's `fmt --check`; this is whitespace-only, no semantic change, so it does not violate "match the brief exactly."
- Verified `cargo test` (7/7 preset tests + 0 others, all green), `cargo fmt --check`, and `cargo clippy --all-targets -- -D warnings` all pass clean before committing.

## Iteration 3

- TDD: wrote `src/dsp.rs` with only the 8-test module first, confirmed RED via `cargo test dsp` (compile errors: `EqChain`/`Filter` undefined), then prepended `db_to_linear`, the `Filter` trait, its `DirectForm1<f64>` impl, `EqChain::{new, from_filters, process, num_bands}`, and the `coefficients` helper; confirmed GREEN (8 passed, 15/15 total).
- biquad-0.6 drift check: verified the installed 0.6.0 source directly — `Coefficients::<f64>::from_params(Type<C>, Hertz<C>, Hertz<C>, C) -> Result<_, Errors>`, `Errors::{OutsideNyquist, NegativeQ, NegativeFrequency}` (derives Debug for `{e:?}`), `ToHertz::hz()`, `DirectForm1::<f64>::new`, `Biquad::run`. The brief's code compiles verbatim; NO adaptation was needed (Fc 30 kHz @ 48 kHz → `OutsideNyquist`, so `rejects_band_above_nyquist` passes).
- RT-safety: `process` and `Filter::run` are alloc/lock/I/O-free (only `chunks_exact_mut`/`iter_mut`/`zip` + f64 arithmetic + register widen/narrow); all boxing/`Vec` allocation happens in `new`/`from_filters`. f64 for all arithmetic/coeffs/state; f32 only at the buffer boundary.
- Gotcha (same as Iteration 2): the brief's verbatim test literals are not byte-identical to `rustfmt` output (struct/`assert_eq!` reflow), so ran `cargo fmt` after pasting to satisfy the hook's `fmt --check`; whitespace-only, no semantic change, 8 tests unchanged behaviorally.
- Verified `cargo test` (15/15 green), `cargo fmt --check`, and `cargo clippy --all-targets -- -D warnings` all pass clean before committing.

## Iteration 4

- `cargo add --dev criterion` (resolved 0.8.2), appended `[[bench]] name = "dsp" harness = false` to `Cargo.toml`; criterion stays dev-only, runtime deps unchanged (cpal/biquad/ringbuf/regex/anyhow).
- Wrote `benches/dsp.rs` (criterion, 10-band fixture via `include_str!("../presets/example.txt")`, black-boxed `chain.process`) and `tests/perf.rs` (ignored-by-default NFR guard: 10 s of 256-frame stereo blocks after 1,000-block warm-up) verbatim from the brief.
- **Measured NFR baseline (this machine):** `cargo bench --bench dsp` → `time: [5.6805 µs 5.6969 µs 5.7156 µs]` (median 5.6969 µs/iter, thrpt ≈ 44.9 Melem/s). `cargo test --release --test perf -- --ignored --nocapture` → `dsp: 0.184% of one core, 9.80 µs per 256-frame block` — PASSED (well under the <1%-of-one-core and <1 ms/block NFR gates; both figures land inside the brief's expected 2–10 µs/block range).
- Confirmed default `cargo test` reports `dsp_meets_cpu_and_latency_budget ... ignored, wall-clock timing: run explicitly with --release` while all 15 other tests stay green — the perf gate never runs in the normal suite/hook.
- Gotcha (same as Iterations 2–3): the brief's verbatim `assert_eq!(parsed.preset.bands.len(), 10, "...")` one-liner in `benches/dsp.rs` exceeds rustfmt's line width and gets reflowed to a multi-line call; ran `cargo fmt` after pasting — whitespace-only, no semantic change.

## Iteration 5

- TDD: wrote `src/devices.rs` with only the 3-test `pick_rate` module first, confirmed RED via `cargo test devices` (compile error: `pick_rate` not found), then prepended `Direction`, `list`, `find`, `pick_rate`, `output_config` and confirmed GREEN (3/3, 18/18 total); added `pub mod devices;` to `src/lib.rs`.
- **cpal-0.18 drift vs. the brief (verified against the installed `cpal-0.18.1` source, `src/lib.rs`/`src/traits.rs`/`src/device_description.rs`), two adaptations, public interface unchanged:** (1) `SampleRate`/`ChannelCount` are now plain type aliases (`= u32`/`= u16`), not newtypes — dropped the `.0` field access on `min_sample_rate()`/`max_sample_rate()` and the `SampleRate(rate)` tuple-constructor call, assigning the bare `u32`/using the returned values directly instead; removed the now-unused `SampleRate` import. (2) `Device`/`DeviceTrait` has no `.name()` method in 0.18 (`DeviceTrait: Display`, name comes from `Display`/`device.description()?.name()`); since `Display::fmt` for the concrete `Device` calls `description()` internally and `to_string()` panics on a `Display` error, added a private `device_name(&Device) -> String` helper built on `device.description().map(|d| d.name().to_string()).unwrap_or_else(|_| "<unnamed>".into())` and used it in both `list` and `find` instead of the brief's `d.name().unwrap_or_else(...)`.
- Verified behavior end-to-end on real hardware, not just compilation: wrote a throwaway `examples/devprobe.rs` calling `list`/`find`/`output_config` against `cpal::default_host()` on this macOS (CoreAudio) machine — printed `MacBook Pro Microphone` / `MacBook Pro Speakers`, found the default output device, and negotiated `StreamConfig { channels: 2, sample_rate: 44100, buffer_size: Default }` with `exact=true`; deleted the example afterward (not part of the brief's interface).
- `pick_rate` kept exactly as specified (pure, no adaptation needed): exact match anywhere in a range wins `(want, true)`; otherwise nearest range endpoint via `abs_diff` wins `(rate, false)`; `None` for empty `ranges`. `output_config` filters `supported_output_configs()` to the requested `channels` and `SampleFormat::F32`, delegates to `pick_rate`, and leaves `buffer_size: BufferSize::Default` for the engine (Task 6) to set after clamping.
- Verified `cargo test` (18/18 green, 1 perf test still correctly ignored by default), `cargo fmt --check`, and `cargo clippy --all-targets -- -D warnings` all pass clean before committing (ran `cargo fmt` once after pasting the brief's tests, same whitespace-only reflow gotcha as prior iterations).

## Iteration 6

- TDD: wrote `src/engine.rs` with only the 3-test `clamp_buffer` module first, confirmed RED via `cargo test engine` (compile error: `clamp_buffer` not found), then prepended the full engine (`EngineConfig`, `clamp_buffer`, `run`) and confirmed GREEN (3/3, 21/21 total); added `pub mod engine;` to `src/lib.rs`.
- **cpal-0.18 drift vs. the brief (two adaptations, public interface unchanged):** (1) `SampleRate` is a bare `u32` alias, not a newtype — `in_default.sample_rate().0` → `sample_rate()`, `out_cfg.sample_rate.0` → `out_cfg.sample_rate`, `StreamConfig { sample_rate: SampleRate(rate), .. }` → `sample_rate: rate`; dropped the unused `SampleRate` import. (2) `build_input_stream`/`build_output_stream` take `config: StreamConfig` **by value** in 0.18 (not `&StreamConfig`) — passed `in_cfg`/`out_cfg` by value (StreamConfig is `Copy`). Confirmed the 4th arg is `timeout: Option<Duration>` and `None` type-checks; `InputCallbackInfo`/`OutputCallbackInfo` and `StreamTrait::play()` names unchanged.
- **ringbuf-0.5 drift: NONE.** Verified against the installed source: `ringbuf::HeapRb`, `ringbuf::traits::{Consumer, Producer, Split}`, `HeapRb::<f32>::new(cap)`, `.split() -> (CachingProd, CachingCons)`, `push_slice(&[T]) -> usize`, `pop_slice(&mut [T]) -> usize` all match the brief exactly.
- RT self-review: both data callbacks are alloc/lock/I/O-free — input does copy→`chain.process`→clip-count→`push_slice` over a pre-allocated `scratch` via `chunks(MAX_CALLBACK_SAMPLES)`; output does `pop_slice`→`fill(0.0)` shortfall. `HeapRb`, scratch, `EqChain`, and prefill silence are all allocated before `play()`. Cross-thread signalling is `AtomicU64` with `Ordering::Relaxed` only; `eprintln!` appears solely in the main-thread stats loop and the error callbacks (fired on device failure, not the steady-state path).
- Verified `cargo test` (21/21 green, 1 perf test ignored by default), `cargo clippy --all-targets -- -D warnings` clean; ran `cargo fmt` after pasting (same whitespace-only reflow gotcha as prior iterations — the brief's tests and the multi-line `eprintln!` reflow to rustfmt's layout, no semantic change).

## Iteration 7

- TDD: wrote `src/cli.rs` with only the 6-test module first, confirmed RED via `cargo test cli` (compile errors: `Cmd`/`parse` undefined), then prepended `Cmd`, `RunArgs` (+ `Default`), `USAGE`, and `parse` verbatim from the brief and confirmed GREEN (6/6, 27/27 total); added `pub mod cli;` to `src/lib.rs`.
- Rewrote `src/main.rs` verbatim from the brief (67 lines, well under the ≤120 budget): thin dispatcher over `cli::parse` → `devices::list` / `install_sink` (stub: bails "install-sink is not implemented yet", Task 8 replaces it) / `run`; `run` reads the preset file, parses it, prints parser warnings, calls `warn_headroom` (PRD 3.1: preamp is the only clip protection — WARNS only, never limits dynamically), finds input/output devices by optional name substring, and calls `engine::run` with the `after_start_hook` (stub: prints "--auto-link is not implemented yet; ignored" when set, returns `None`; Task 9 replaces it).
- No cpal-0.18 adaptation needed here (unlike Tasks 5/6) — `main.rs` only touches `cpal::default_host()`, which is unaffected by the `SampleRate`/`.name()` drift already absorbed inside `devices`/`engine`.
- Verified `cargo test` (27/27 green, 1 perf test still correctly ignored by default), `cargo clippy --all-targets -- -D warnings` clean; ran `cargo fmt` after pasting the brief's tests (same whitespace-only reflow gotcha as prior iterations — `assert!(matches!(...))` and the `"run", "--preset", ...` args slice exceed rustfmt's line width, no semantic change). Manually ran `cargo run -- help` (prints `USAGE` verbatim) and `cargo run --release -- devices` (lists `MacBook Pro Microphone` / `MacBook Pro Speakers`) — both confirmed dispatch works end-to-end.
- **Deferred the live-audio `run` check** (brief Step 6): this machine's only devices are the built-in `MacBook Pro Microphone` and `MacBook Pro Speakers` — starting `run` would immediately capture live mic input and play it back through the laptop's own speakers with no way to verify system volume/mute state beforehand or guarantee a clean kill before audible feedback, which the brief explicitly permits deferring ("if you cannot do so safely/headlessly, DEFER the live-audio check"). The 27 automated tests (including the 6 new CLI tests) plus the verified `help`/`devices` dispatch are the recorded evidence instead; a future iteration with a safe monitoring setup (e.g. BlackHole loopback + muted output, or Linux/PipeWire per Task 8/9) should perform this check.

## Iteration 8

- TDD: wrote `src/routing.rs` with only the 3-test module first, confirmed RED via `cargo test routing` (compile errors: `sink_config`/`config_path`/`SINK_NODE` not found), then prepended `SINK_NODE`, `sink_config`, `config_path`, `install_sink` verbatim from the brief and confirmed GREEN (3/3, 30/30 total); added `pub mod routing;` to `src/lib.rs`.
- Design Decision 7 honored: `routing.rs` uses only `std::{fs, path, env}` + `anyhow` — no platform-specific crates, no `#[cfg]`-gating — so `sink_config`/`config_path` compile and unit-test on this macOS machine exactly as they will on Linux. The one OS-specific behavior (actually being useful) is fenced at the call site in `main.rs` via a **runtime** `cfg!(target_os = "linux")` check, not a compile-time `#[cfg]`, per the brief's explicit instruction.
- Replaced the `install_sink` stub in `src/main.rs`: on non-Linux it now `bail!`s with the macOS/BlackHole guidance (verbatim from the brief); on Linux it calls `oxideq::routing::install_sink()` via full path (no change to the `use oxideq::{...}` list needed). Manually ran `cargo run -- install-sink` on this macOS box and confirmed it prints the BlackHole guidance and exits non-zero — the Linux branch is never reached here, as expected.
- Verified `cargo test` (30/30 green, 1 perf test still correctly ignored by default) and `cargo clippy --all-targets -- -D warnings` clean; ran `cargo fmt` after pasting the brief's tests (same whitespace-only reflow gotcha as prior iterations — the `config_path_honors_xdg_config_home` one-line `assert_eq!` exceeds rustfmt's line width, no semantic change).
- **Deferred the Linux/PipeWire manual check** (brief Step 6): no Linux/PipeWire machine is available here, so `cargo run --release -- install-sink` + `systemctl --user restart pipewire pipewire-pulse` + `pw-cli ls Node | grep -A2 OxidEQ` were not run. The three unit tests (`sink_config_defines_a_null_sink_named_oxideq`, `config_path_defaults_to_dot_config`, `config_path_honors_xdg_config_home`) cover the generated config text and path resolution instead; a future iteration on a Linux box should perform this check and confirm the `OxidEQ-Sink` node shows up in `pw-cli`/the desktop output picker.

## Iteration 9

- TDD: appended the 3 new `matching_ports` tests to the existing `routing::tests` module first, confirmed RED via `cargo test routing` (compile errors: `matching_ports` not found in scope, 4 errors — one per test call site), then added `pub fn matching_ports`, `pub fn auto_link`, and the private `pw_link_list`/`link`/`unlink_feedback` helpers verbatim from the brief and confirmed GREEN (6/6 routing tests, 33/33 total); extended the `use` lines with `std::process::Command`, `std::time::Duration`, and `anyhow::bail` as specified.
- Replaced the `after_start_hook` stub in `src/main.rs` verbatim from the brief: `!a.auto_link` → `None`; non-Linux → warn "--auto-link is Linux/PipeWire only; ignored" + `None` (runtime `cfg!(target_os = "linux")` check, not `#[cfg]`, per the design intent so the module keeps compiling/testing on macOS); Linux → clones `a.output` into a boxed closure that calls `oxideq::routing::auto_link(dac.as_deref())` and on error prints the failure plus a fallback-to-qpwgraph (Tier 2) hint.
- Design intent honored: `routing.rs` stays std-only (`fs`, `path`, `process::Command`, `time::Duration`) + `anyhow` — no new runtime deps added, matching the "cpal/biquad/ringbuf/regex/anyhow only" global constraint. `auto_link`/`pw_link_list`/`link`/`unlink_feedback` are NOT `#[cfg]`-gated — they compile on all platforms (verified: `cargo build` and full `cargo test` both succeed on this macOS box) and are only ever reached at runtime through the Linux-gated `after_start_hook`. The feedback guard (`unlink_feedback`) unconditionally cuts any `oxideq → OxidEQ-Sink:playback_FL/FR` link after wiring, using `let _ = ...status()` to ignore the exit code (best-effort, per Design Decision 9).
- Gotcha (same as Iterations 2–8): the brief's verbatim test/match-arm literals are not byte-identical to `rustfmt` output (the `usb-DAC` `vec![...]` assertion, the sorted-FL/FR `assert_eq!`, and the `None => eprintln!(...)` arm all reflow) — ran `cargo fmt` after pasting; whitespace-only, no semantic change. Verified `cargo test` (33/33 green, 1 perf test still correctly ignored), `cargo clippy --all-targets -- -D warnings`, and `cargo fmt --check` all pass clean before committing.
- **Deferred the Linux/PipeWire manual verification** (brief Step 6): no Linux/PipeWire machine is available here, so `cargo run --release -- run --preset presets/example.txt --output "<DAC name>" --auto-link` followed by `pw-link -l | grep -i oxideq` were not run — this exercises live `pw-link` process invocation and real graph wiring that cannot be simulated headlessly on macOS. The 3 new unit tests (`matching_ports_filters_by_node_case_insensitively`, `matching_ports_is_sorted_fl_before_fr`, `matching_ports_matches_node_not_port_text`) cover the pure parsing/sorting/node-vs-port-matching logic that is `auto_link`'s core correctness oracle; a future iteration on a Linux/PipeWire box should perform the live check and confirm no `oxideq → OxidEQ-Sink:playback_*` feedback link remains after wiring.
